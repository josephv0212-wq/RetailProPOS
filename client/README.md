
  # Sales Page POS Interface

  This is a code bundle for Sales Page POS Interface. The original project is available at https://www.figma.com/design/qxSpfePYJCXP8YM63c9ZYG/Sales-Page-POS-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  